#!/usr/bin/env node

console.error(
  "shopify-operations-mcp: server not implemented yet — the MCP server lands in later tickets (see https://github.com/jpka/shopify-operations-mcp/issues/3).",
);
process.exitCode = 1;
